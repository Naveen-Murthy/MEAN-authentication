{
  "name": "backend",
  "version": "1.0.0",
  "description": "MEAN stack authentication app backend",
  "main": "app.js",
  "scripts": {
    "devstart": "babel-node ./build/app",
    "clean": "rm -rf ./build && mkdir build",
    "build": "npm run clean && npm run babel-build",
    "start": "npm run build && node ./build/app",
    "babel-build": "babel app.js -d build",
    "heroku-postbuild": "npm install && npm run build"
  },
  "author": "Murthy Majji",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.3.3",
    "passport": "^0.5.3",
    "passport-jwt": "^4.0.0",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@babel/cli": "^7.17.10",
    "@babel/core": "^7.18.0",
    "@babel/node": "^7.17.10",
    "@babel/preset-env": "^7.18.0",
    "babel-watch": "^7.7.0"
  }
}
